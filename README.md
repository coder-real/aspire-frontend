# Aspire Frontend

Next.js 14 (App Router) frontend for the Aspire school management platform.

## Local setup

1. Clone the repo
2. `npm install`
3. Copy `.env.example` to `.env.local` and fill in values
4. `npm run dev`

App runs at `http://localhost:3000`

## Scripts

| Command | What it does |
|---|---|
| `npm run dev` | Start dev server |
| `npm run build` | Production build |
| `npm start` | Serve production build |

## Linked repos

- Backend: [aspire-backend](https://github.com/YOUR_USERNAME/aspire-backend)
